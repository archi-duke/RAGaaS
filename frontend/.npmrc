# KendoReact 5.8.0 은 peerDependencies 가 react ^18 까지라 React 19 설치가 peer 충돌로 막힌다.
# 플랫폼 계약(01 §4)상 react 19 정렬이 필수이고, Kendo 5.8 은 React 19 런타임에서 동작이
# 실측 검증됨(ragaas-onboarding-prompt.md §1) — peer 검사만 완화한다.
# Kendo 5.8.0 이후 버전 상향 금지(라이선스 구독 만료 2024-04-05).
legacy-peer-deps=true
